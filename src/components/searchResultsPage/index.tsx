import React, { FC } from 'react';

export interface someProps {};

const ResultsPage: FC<someProps> = ({}) => {

    return (
        <div>
            <h1>Results Page</h1>
        </div>
    );
}

export default ResultsPage;